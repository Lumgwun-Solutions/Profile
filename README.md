# Lumgwun Solutions & Awajimaa Group Profile

The public company profile for [profile.awajimaa.com](https://profile.awajimaa.com).

Cloudflare Pages deploys the verified production site automatically from the `main` branch. The committed `public/` directory is the build output and Pages runs the lightweight `npm run build` command before serving it.
